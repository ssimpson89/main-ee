---
name: Release

on:
  release:
    types: [created]

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      version_tag: ${{ github.event.release.tag_name }}
      moving_tag: latest
