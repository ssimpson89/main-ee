---
name: Build dev images

on:
  workflow_dispatch:
  # schedule:
  #   - cron: '0 */12 * * *'

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      version_tag: dev
